# Core dependencies for the CAD Data Lakehouse Pipeline
boto3>=1.28.0
requests>=2.31.0
pyspark==3.5.0
delta-spark>=3.0.0
pandas>=2.0.0
dbt-spark[session]>=1.7.0

# CAD Processing (install via conda: conda install -c conda-forge pythonocc-core=7.8.1)
# pythonocc-core is not pip-installable; it requires conda
